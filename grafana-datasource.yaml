apiVersion: 1

datasources:
  - name: Schwab SQLite
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    editable: true
    jsonData:
      path: /databases/schwab.db
  - name: Alpaca SQLite
    type: frser-sqlite-datasource
    access: proxy
    isDefault: false
    editable: true
    jsonData:
      path: /databases/alpaca.db
